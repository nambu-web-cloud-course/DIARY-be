mydiary - gallery

-사용자가 올린 사진은 디비에서 매 사진별 고유값을 가지므로 1:N 이다.

Calender
-todolist, mydiary 에서 데이터를 가져와서 캘린더에 뿌려주는 형태로 진행

bg_theme()
-사용자가 테마를 선택함과 동시에 일기컨텐츠 한 부분이 되므로 관계가 필요없음.
(html의 한 태그로 들어가는 것이지 고유한 아이디를 가진 데이터로서 들어가는 것이 아니므로...)

[스터디 필요 기능]
1.pdf 다운로드 기능

[할일]

1. bg_theme 테이블 생성(모델) 및 기초데이터 입력(디비에서 insert)

2. cate_data 테이블 생성(모델) 및 기초데이터 입력

3. mydiarty 테이블에 cate_data 칼럼 만들기

4. gallery 테이블 생성(모델) 및 gal_router 만들기